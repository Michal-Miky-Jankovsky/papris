{
  "name": "papris",
  "version": "1.0.0",
  "dependencies": {
    "chai": "^3.5.0",
    "ejs": "^2.5.6",
    "eslint": "^2.0.0-rc.1",
    "express": "^4.15.2",
    "immutable": "^3.8.2",
    "mocha": "^3.2.0"
  },
  "description": "paper tetris for children",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec",
    "testDebug": "./node_modules/.bin/mocha --debug-brk --inspect --reporter spec"
  }
}
